cin:忽略开头所有空白，包括空格、换行、制表符，读取字符，直到遇到空白字符
getline：读取字符，直到遇到第一个换行符，且读取的字符不包括最后遇到的换行符

在读取时：string对象的长度类型为string::size_type，而不是int，string.size(),返回string的长度，不要把这个长度赋值给int型。
字符串赋值过程：str1 = str2;把str1的相关类存释放，分配个str1足够装下str2的类存，把str2的内容复制到str1的空间中去

字符串连接符号+，其左右操作数必须至少有一个string类型，且其返回值为string类型
